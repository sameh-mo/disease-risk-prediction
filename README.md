# disease-risk-prediction
A machine learning project to predict liver disease using patient medical attributes. It uses KNN and Decision Tree models, compares their performance, and evaluates results. The workflow includes data loading, EDA, preprocessing, encoding categorical features, and splitting data into train/test sets to build a reliable classification model.
